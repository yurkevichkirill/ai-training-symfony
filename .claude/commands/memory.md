---
name: memory
description: "Refresh applicable repository-local context without changing governed task authority."
---

# Memory

This command accepts no arguments. If $ARGUMENTS is not empty, stop and ask
the user to invoke `/memory` without arguments.

Read `.claude/skills/memory/SKILL.md`, execute exactly one unified repository
memory refresh in the current agent, and stop.
